import PaymentHistory from "@/app/components/paymentHistory/PaymentHistory";

const page = () => {
    return (
        <div>
            <PaymentHistory/>
        </div>
    );
};

export default page;